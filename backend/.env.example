# Copie para .env na mesma pasta (backend) e ajuste se precisar.
# SQLite local (padrao do projeto hoje; connect_args em database.py e para SQLite)
DATABASE_URL=sqlite:///./scge.db

# PostgreSQL (quando migrar):
# DATABASE_URL=postgresql+psycopg2://usuario:senha@localhost:5432/scge
